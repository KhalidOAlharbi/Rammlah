# Local development when the backend is running separately:
VITE_API_BASE_URL=http://localhost:8000

# On DigitalOcean, leave this unset when the backend and dashboard are
# deployed in the same app with /api routed to the backend.
